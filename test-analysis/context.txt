# Service Desk — технический долг (сентябрь 2026)

## Текущее состояние

- **Фронт**: 485 тестов (102 suite)
- **Сервер**: 523 тестов (35 suites)
- **E2E**: 17/17 страниц ALL OK (check-console.mjs) + 16 spec'ов (playwright)
- **Prisma моделей**: 25 (включая event_outbox, custom_field_definitions, custom_field_values, api_tokens, webhooks, ticket_recurrences, automation_rules)
- **API endpoints**: 90+ (Swagger + Health probes + k6 metrics + IMAP admin routes + API tokens + Webhooks + Lock + Recurrences + Rules + SSO)
- **ESLint**: 0 errors, 2 warnings (react-hooks/set-state-in-effect)
- **MySQL**: запущен (Laragon), БД servicedesk
- **API**: порт 4000, Vite proxy 5173
- **check-console**: пройден — 17 страниц без console.error, русский текст есть
- **i18n**: исправлены дубли auth ключей, русификация ForgotPassword/ResetPassword/SSO (Этап 36)
- **Известные баги**: 86/570 тестов падают из-за vitest конфига (нужен `vitest.config.ts` с `environment: 'jsdom'`)

## Ключевые файлы

### Сервер (server/src/)
| Файл | Coverage | Статус |
|------|----------|--------|
| routes/tickets.js | ~75% | ✅ (+webhook триггеры) |
| routes/auth.js | ~73% | ✅ |
| routes/admin.js | ~79% | ✅ |
| routes/notifications.js | ~73% | ✅ |
| routes/push.js | ~66% | ✅ |
| routes/search.js | 100% | ✅ |
| routes/calendar.js | ~66% | ⚠️ |
| routes/api-tokens.js | новый | ✅ CRUD API-токенов |
| routes/webhooks.js | новый | ✅ CRUD webhooks (admin-only) |
| services/tickets.service.js | ~91% | ✅ |
| services/api-tokens.service.js | новый | ✅ SHA-256 hash, validate, list, delete |
| services/webhooks.service.js | новый | ✅ 7 событий, HMAC, retry 3x |
| services/collision.service.js | новый | ✅ Lock/unlock/force, 30мин TTL |
| services/recurrence.service.js | новый | ✅ Cron-parser, processRecurrences |
| services/rules.service.js | новый | ✅ Evaluate conditions, execute actions |
| auth/oidc.js | новый | ✅ OIDC discovery, auto-provisioning |
| middleware.js | - | ✅ JWT + API token auth |
| socket.js | ~22% | ❌ (низкий приоритет — outbox паттерн снижает риски) |
| background.js | ~50% | ✅ DLQ реализован (BullMQ + in-memory fallback) |
| api.test.js | n/a | 145 интеграционных |

### Клиент (src/)
| Файл | Coverage | Статус |
|------|----------|--------|
| Dashboard.tsx | 100% | ✅ |
| Admin.tsx | 98% | ✅ |
| Tickets.tsx | 90% | ✅ |
| Calendar.tsx | 84% | ✅ |
| Files.tsx | 84% | ✅ |
| News.tsx | 80% | ✅ |
| Polls.tsx | 74% | ✅ |
| Employees.tsx | 56% | ⚠️ |
| ChatDetail.tsx | 51% | ⚠️ |
| TicketDetail.tsx | 51% | ⚠️ |
| Kanban.tsx | 47% | ⚠️ |
| ticket-context.tsx | 35% | ❌ |
| Search.tsx | ~60% | ⚠️ |
| AdminSettings.tsx | - | ✅ IMAP + Email Templates + Feature Flags |
| AdminIntegrations.tsx | новый | ✅ API Tokens + Webhooks UI |
| AdminRules.tsx | новый | ✅ Rules Engine CRUD UI |
| SSO.tsx | новый | ✅ SSO Login + Callback pages |
| CommandPalette.tsx | новый | ✅ Cmd+K навигация + поиск |

## Проблемы (решённые)

### MySQL не запущен → 500 на всех страницах
- **Причина**: Laragon был запущен, но MySQL-сервис не стартовал
- **Решение**: перезапущен Laragon → MySQL поднялся
- **Дополнительно**: `deleted_at`, `last_active`, `event_outbox` не существовали в БД — добавлены вручную

### Prisma client не синхронизирован со схемой
- **Причина**: после мержа 10 коммитов (soft-delete, outbox) `prisma generate` не был запущен
- **Решение**: `npx prisma generate` + ALTER TABLE для добавления колонок

## Команды
```bash
cd E:\Tickets cursor\ticketscursor
npm run test                  # фронт
npx vitest run --coverage     # coverage фронт
cd server && npm test         # сервер
npx vitest run --coverage     # coverage сервер
npm run build                 # production сборка
node check-console.mjs        # E2E проверка (требует MySQL + оба dev-сервера)
```

## Аудит по Промты.txt

Внедрено:
- ✅ Outbox Pattern — `event_outbox` таблица + Worker
- ✅ Soft Deletes — `deleted_at` на tickets, ticket_messages, files, chat_messages
- ✅ Graceful Shutdown — SIGTERM/SIGINT handler
- ✅ Health check + Readiness probe — `/api/health`, `/api/ready`
- ✅ CSP (Helmet strict) — connect-src разрешён localhost:*
- ✅ Audit log — `audit_log` таблица + middleware
- ✅ Load testing (k6) — сценарии в `k6/`
- ✅ Grafana dashboard — мониторинг API/WS/Redis
- ✅ Bundle size check в CI — лимит 2MB

Реализовано (v1.4.0):
- ✅ Email Templates — 8 шаблонов в админке, валидация JSON, SMTP из БД
- ✅ Email Notifications — все 6 типов (create/status/priority/assign/message/SLA)
- ✅ notifyPriorityChanged — email + in-app (раньше только Telegram)
- ✅ notifyTicketMessage — email создателю + исполнителю

Реализовано (v1.3.0):
- ✅ Bull Queue DLQ (background jobs) — BullMQ retry+DLQ + in-memory withRetry+DLQ
- ✅ last_active в employees (миграция 20260720)
- ✅ Idempotent polls migration (20260710)
- ✅ Feature Flags (Этап 29)

Остаётся не реализовано:
- ✅ API Versioning (через middleware в app.js: /api/v1/ → /api/ + заголовок X-API-Version: v1)
- ❌ Visual regression testing
- ✅ Read receipts для чатов — РЕАЛИЗОВАНО в Этапе 25: пер-юзерный курсор `chat_read_receipts`, WS `chat:read` + REST PUT /:id/read (lastReadMessageId), синие ✓ в ChatDetail.tsx для прочитанных
- ✅ Bulk actions (массовое назначение/закрытие) — Этап 2: реализовано (POST /api/tickets/bulk: status/assign/priority, чекбоксы + панель в Tickets.tsx)
- ✅ Шаблоны ответов (canned responses)
- ✅ Time tracking
- ✅ Tags/labels на тикетах (JSON `tags`, create/редактирование в TicketDetail, фильтр и отображение в Tickets, `PUT /api/tickets/:id/tags`)
- ✅ Mentions (@username) — Этап 3 (см. дорожную карту)
- ✅ ИИ-ответы из Wiki (assistant.service.js: extractKeywords/searchWiki/buildAnswer/generateLlmAnswer; POST /api/tickets/:id/assistant; карточка в TicketDetail для staff; 7 ключей i18n; 4 теста)
- ✅ SLA breach escalation

## Сравнение с аналогами рынка

Файл `Service Desk против аналогов рынка.md` — сравнительный анализ (Zammad, GLPI, osTicket, Zendesk, Кайтен, Okdesk и др.), оценка стека (Node/Express + Prisma + MySQL + Socket.IO + Redis — уникален среди open-source helpdesk'ов), уникальная комбинация модулей (WS-чаты в тикетах + Wiki + календарь + PWA + Tauri + K8s), рекомендация: развивать свою систему по тикетному роадмапу, аналоговые фичи (видео/SIP/E2E) — переросток.
Источник данных: `Аналоги.rtf` / `Аналоги.txt`.

## Дорожная карта (тикетные фичи, по одной, зелёный прогон после каждой)

- ✅ Этап 1 — Tags/labels (см. выше)
- ✅ Этап 2 — Bulk actions (POST /api/tickets/bulk + чекбоксы/панель в Tickets.tsx)
- ✅ Этап 3 — Mentions (@username) — автомкомплит в TicketDetail (@Имя Фамилия → вставка), подсветка упоминаний, уведомления упомянутым (notifyTicketMention, type=ticket_mention); backend: миграция `20260724_ticket_mentions.js` (JSON `mentions` на ticket_messages), `resolveMentionedEmployees` в tickets.service.js (матч по полному имени или local-part email, sender исключён), роут POST /:id/messages сохраняет mentions; swagger обновлён
- ✅ Этап 4 — Read receipts + offline queue (миграция `20260725_chat_read_receipts.js`; курсор на чат; WS ack `message:ack`; localStorage-outbox в SocketContext с flush при реконнекте; реконсиляция эха собственных сообщений в ChatDetail; точная missed-доставка по курсору вместо last_active)
- ✅ Этап 5 — Time tracking (см. раздел ниже)
- ✅ Этап 6 — API versioning (middleware app.js /api/v1/ → /api/, cache bugfix, cache.test.js 6/6)
- ✅ Этап 7 — ИИ-ответы из Wiki (assistant.service.js, POST /:id/assistant, TicketDetail UI, 4 теста)
- ✅ Этап 36 — i18n: исправлены дубли auth ключей, русификация ForgotPassword/ResetPassword/SSO

## Admin Operations — `/admin/settings`

Страница "Настройки" теперь содержит блок **"Операции"**:

| Операция | Что делает | API |
|----------|-----------|-----|
| **Redis** | Показывает статус (ping), позволяет ввести/сохранить REDIS_URL | `GET /admin/settings/redis-status`, `PUT /admin/settings/redis` |
| **Backup** | Запускает `scripts/backup-mysql.ps1` (mysqldump, retention 7d) | `POST /admin/settings/backup` |
| **Seed** | Запускает `npm run db:seed` в server/ | `POST /admin/settings/seed` |
| **Geo** | Создаёт FULLTEXT index на tickets (title, description) | `POST /admin/settings/geo` |

Файлы: `server/src/routes/admin.js`, `src/pages/AdminSettings.tsx`, `scripts/backup-mysql.ps1`

## Feature Flags — реализация (Этап 29)

**Backend:**
- Миграция `server/migrations/20260720_feature_flags.js` — таблица `feature_flags` (key PK, enabled, description, updated_at)
- `GET /api/admin/features` — кэшируется 30с через `cacheMiddleware`
- `PUT /api/admin/features` — bulk update флагов, инвалидирует кэш
- Prisma модель `feature_flags` в `schema.prisma`

**Frontend:**
- `src/hooks/useFeature.ts` — хук `useFeature(key)` возвращает boolean (default true), использует React Query со staleTime 30s
- `FeatureFlagsSection` в `AdminSettings.tsx` — toggle-переключатели для каждого флага, кнопки Save/Reset
- i18n ключи `admin.features`, `admin.featuresSubtitle`, `common.reset`

**Тесты:**
- Сервер: 5 тестов (GET defaults, PUT update, GET after update, non-admin rejection, invalid body)
- Клиент: 3 теста (useFeature, useAllFeatures) + 2 теста (feature section rendering, toggle)
- Фронт: 52 файла / 372 теста, сервер: 25 файлов / 352 теста

## SLA Escalation — реализация (v1.5.0)

**Что делает:**
- При просрочке SLA (due_at истёк) и включённой эскалации — автоматически повышает приоритет тикета
- Уровни: low → medium → high → critical (повышение на 1 шаг за каждый интервал эскалации)
- Настраивается в админке: включение/выключение, шаг эскалации в часах

**Backend:**
- Миграция `server/migrations/20260721_sla_escalation.js` — `escalated_at` + `escalation_level` в tickets
- Prisma модель: `escalated_at DateTime?`, `escalation_level Int @default(0)`
- `server/src/background.js` — `runSlaCheck()` расширена: проверяет каждый просроченный тикет на необходимость эскалации
- `server/src/notify.js` — новая функция `notifySlaEscalated()`: уведомления (in-app + email + Telegram), аудит-лог, WebSocket `ticket:sla:escalated` в комнату тикета
- Настройки `SLA_ESCALATION_ENABLED` (true/false) и `SLA_ESCALATION_HOURS` (число) через AllowedSettings

**Frontend:**
- `TicketDetail.tsx` — бадж "Эскалация ур. N" (красный, AlertTriangle) при escalationLevel > 0
- `AdminSettings.tsx` — поля `slaEscalationEnabled`, `slaEscalationHours` в секции ticketSettings
- i18n ключи: `tickets.escalated`, `admin.slaEscalationEnabled`, `admin.slaEscalationHours`

## Email Notifications — реализация (v1.4.0)

**Email Templates UI:** `AdminSettings.tsx` — 8 шаблонов (create/status/priority/assign/message/SLA breach), textarea-редакторы, кликабельные `{{variable}}` теги, сброс на дефолт

**Backend validation:** `PUT /admin/settings` проверяет EMAIL_TEMPLATES JSON + все 8 ключей

**SMTP from DB:** `email.js` читает из `admin_settings` (БД), fallback на `process.env`; пересоздаёт транспортер при смене конфига

**Startup warning:** `index.js` — предупреждение, если SMTP_HOST/USER/PASS не заданы

**Email coverage:**
- `notifyTicketCreated` — email создателю ✅
- `notifyStatusChanged` — email создателю + исполнителю ✅
- `notifyPriorityChanged` — email создателю + исполнителю ✅ (новое)
- `notifyTicketAssigned` — email исполнителю ✅
- `notifyTicketMessage` — email создателю + исполнителю ✅ (новое)
- `notifySlaBreached` — email всем админам ✅

## Time tracking — реализация (v1.8.0, Этап 5)

**Что делает:** карточка «Время» в TicketDetail для agent/senior_agent/admin/super_admin — ручной ввод времени по тикету + таймер (start/stop) с живым подсчётом.

**Backend:**
- Миграция `server/migrations/20260918_time_tracking.js` — таблицы `time_entries` (ticket_id, user_id, minutes, description, entry_date) и `ticket_timers` (ticket_id, user_id, started_at), FK, индексы
- Prisma: модели `TimeEntry`, `TicketTimer` (+ relation на Ticket/User), `tickets.time_spent_minutes Int @default(0)`
- `server/src/services/time.service.js` — `createTimeEntry`, `getTicketTime` (entries + totalMinutes + totalEntries + activeTimer), `startTimer`/`stopTimer` (стоп пишет entry из потраченных минут, ноль не сохраняется), `deleteTimeEntry` (владелец или senior_agent+), пересчёт `time_spent_minutes` на постоянный инвариант
- `server/src/routes/tickets.js` — `GET/POST /api/tickets/:id/time`, `DELETE /api/tickets/:id/time/:entryId`, `GET /api/tickets/:id/time/timer`, `POST /api/tickets/:id/time/timer/start|stop`; все под `requireRole('admin','senior_agent','agent')`; схемы в `schemas.js` (minutes 1..1440, description ≤500)
- `tickets.service.js` — `getTicketById` возвращает `time_spent_minutes`

**Frontend:**
- `src/components/TimeTrackingCard.tsx` — итого «X ч Y мин», запуск/остановка таймера (тик 1с), форма минуты+описание (валидация 1..1440 → toast.error), список записей с удалением (свои или admin/senior_agent); data-testid: time-card, time-total, time-start-timer, time-stop-timer, time-timer, time-minutes, time-description, time-add-button, time-entry, time-delete, time-empty
- `TicketDetail.tsx` — карточка подключена после management-блока; `timeSpentMinutes` маппится в `mapTicketDetail`
- MSW-хендлеры для всех time-эндпоинтов; тесты в `TicketDetail.test.tsx` (+4)

**Тесты:** сервер 523/523, клиент 485/485 (102 файла); tsc чист, vite build OK, lint 0 ошибок
- **Известный баг**: 86/570 тестов падают из-за vitest конфига (нужен `vitest.config.ts` с `environment: 'jsdom'`)

## Email Ingestion (IMAP) — реализация (v1.9.0+, ~60%)

**Что делает:** письма на support@ → автоматически создаются тикеты. Ответы на письма → комментарии к тикету (threading через In-Reply-To).

**Backend:**
- `server/src/services/email-ingestion.service.js` — IMAP polling (`imapflow`), парсинг писем (`mailparser`), `extractTicketReference` (ищет `[Ticket #N]` в теме), `stripTicketReference`, `getImapConfig`, `processEmailMessage` (создание тикета + employee по email), `testImapConnection`, `getEmailStats`
- Миграция `024_email_ingestion.sql` — колонка `email_message_id VARCHAR(500)` на `tickets` для thread-связки
- `server/src/routes/admin.js` — `GET /api/admin/imap/status` (статистика писем), `POST /api/admin/imap/test` (проверка IMAP-соединения)
- `ALLOWED_SETTINGS` расширен: `IMAP_HOST`, `IMAP_PORT`, `IMAP_USER`, `IMAP_PASS`
- `server/src/background.js` — `startImapPolling()` при старте сервера, `stopImapPolling()` при shutdown

**Осталось (~40%):**
- Reply threading (In-Reply-To → поиск тикета по `email_message_id`)
- HTML/plain-text multipart парсинг
- Admin UI для IMAP settings (в `AdminSettings.tsx`)
- Attachment handling (вложения → files)
- Тесты reply threading

**Тесты:** 15 unit-тестов `email-ingestion.service.test.js` (extractTicketReference, stripTicketReference, getImapConfig)

## SLA Pause / Business Hours — реализация (v1.9.0)

**Что делает:** SLA-таймеры считаются только в рабочие часы (Mon-Fri 9:00-18:00). Статус «Ожидает ответа клиента» → SLA на паузе.

**Backend:**
- `server/src/services/sla.service.js` — `addBusinessHours()`, `isWithinBusinessHours()`, `getBusinessHoursSettings()`
- `server/src/routes/tickets.js` — проверка `sla_paused_at` при обновлении статуса
- `server/src/background.js` — SLA check учитывает бизнес-часы

## CSAT — реализация (v1.9.0)

**Что делает:** после закрытия тикета отправляется email-опрос «оцените решение» (1-5 звёзд). Метрика в дашборде.

**Backend:**
- `server/src/services/csat.service.js` — `createCsatSurvey()`, `getCsatResponses()`, `getCsatStats()`
- `server/src/routes/tickets.js` — интеграция при статусе `resolved`
- `server/src/email.js` — `sendCsatSurvey()`
