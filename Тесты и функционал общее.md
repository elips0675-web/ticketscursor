# Тесты и функционал общее — свод аудитов qwen · дипсик · kimi

> **Дата**: 24.09.2026 (обновлено после закрытия Этапа 59 «Resilience»)
> **Источники**: «Тесты и функционал qwen.rtf» (24.09), «Тесты и функционал дипсик.rtf» (24.09), «Тесты и функционал kimi.docx» (23.09), «Оценка ии.rtf» (24.09), «Доделать.rtf»
> **База, против которой писались аудиты**: **1451 тест, 0 failures** — клиент 65 файлов / 573, сервер 61 файл / 878; E2E 19 spec'ов / 57 тестов. Coverage: клиент 69.13/61.21/60.12/71.69, сервер 71.29/64.64/71.56/72.29 (пороги 66/58/57/69 и 67/61/65/68 пройдены).
> **Правило**: каждый пункт сведён один раз; в скобках — источник (qwen / дипсик / kimi) и этап плана 54–66, если запланирован.

---

## 1. Состояние проекта — что уже закрыто

| Что | Статус |
|-----|--------|
| Этап 54 — socket.js coverage 22%→82% | ✅ 22.09 |
| Этап 55 — E2E lifecycle (tickets/rbac/sla/websocket), 57/57 | ✅ 23.09 |
| Этап 56 — IDOR-аудит (:id-роуты, requester-фильтр) | ✅ 23.09 |
| Этап 57 — N+1/индексы + Race-тесты (lock/status/timer/outbox), каскады FK «только отчёт» | ✅ 23.09 |
| Этап 58 — Админка (11 фич): health, backup/restore, RBAC-матрица, audit фильтры+CSV, queue monitor, sessions revoke, email-preview, reindex, migrations status, rollout, rate-limit override | ✅ 24.09 (клиент 65/573 + сервер 54/832 = 1405) |
| **Этап 59 — Resilience** (прод-риски из «Финальных оценок 23.09» и «Оценка ии.rtf»): JWT-rotation (sign=new, verify=[old,new]), idempotency вне Redis (in-memory fallback), транзакции multi-step (bulk/assign/escalation), каскады FK (32 FK, идемпотентная миграция), SLA/TZ-баг (TIMEZONE + Intl/TZ + DST), refresh-token family (отзыв всей семьи + revoke-all), trust proxy (TRUST_PROXY=1|true, спуфинг-тесты), **+ oidc/background.sub/идемпотентность/ротация тесты** | ✅ **24.09 — закрыт** (клиент 65/573 + сервер 61/878 = **1451 тест**, lint 0/57, type-check/build ✅, check-console 17/17) |
| Рассинхрон цифр в шапке/подвале frontend-tests.txt, server-tests.txt | ✅ устранено: инвентарь регенерируется из фактического vitest (правило 10), подвалы синхронизированы |
| Пункты «Оценка ии.rtf» №2–4 (refresh-reuse, trust proxy, SLA/TZ) | ✅ закрыты в Этапе 59 — см. раздел 13 |

---

## 2. Тесты — остаток (свод P1/P2)

### P1 — закрыть первыми (распределены по Этапам 60–61)

| # | Задача | Файл | Оценка | Источник | Куда |
|---|--------|------|--------|----------|------|
| 1 | Race-тесты (параллельные lock/status/timer/outbox) | ticket-lock.race, ticket-status.race, time-timer.race, outbox.delivery | 4 файла / ~14 тестов | kimi, дипсик, qwen | ✅ закрыт в Этапе 57 |
| 2 | auth/oidc.test.js (discovery, callback, auto-provisioning, 500/400) | server/src/auth/oidc.test.js | ~10 тестов | kimi, дипсик | ✅ **закрыт в Этапе 59** (+46 серверных тестов) |
| 3 | ticket-context.optimistic — 35% критичного контекста (optimistic insert + rollback при 500/403, invalidateQueries) | src/test/ticket-context.optimistic.test.tsx | ~8–10 тестов | kimi, дипсик, qwen | ⏳ Этап 61 |
| 4 | E2E-реализм: export (download+BOM), file-upload (setInputFiles→сверить файл), search (результат из seed) | e2e/export-real, file-upload-real, search-real | 3 спека / ~8 тестов | kimi, дипсик | ⏳ Этап 61 |
| 5 | i18n parity — ключи ru.json ↔ en.json идентичны | src/test/i18n.parity.test.ts | 1 тест | kimi, дипсик | ⏳ Этап 60 |
| 6 | XSS/injection — `<script>` в title, SQL в search → 400, БД не падает | server/src/__tests__/injection.test.js | ~6 тестов | kimi, дипсик | ⏳ Этап 61 |

### P2 — желательно

| # | Задача | Источник | Куда |
|---|--------|----------|------|
| 1 | a11y: aria-label на icon-only, htmlFor+id, Tab по Sidebar, Esc→Sheet, focus-trap в Radix Dialog | kimi, дипсик, qwen | ⏳ Этапы 60–61 |
| 2 | Background sub-тесты: SLA-эскалация по интервалам, cleanup >90д, DLQ >10/час → email, stopImapPolling идемпотентен | kimi, дипсик | ✅ **закрыт в Этапе 59** (background.sub.test.js) |
| 3 | Virtual scroll в jsdom: 1000 сообщений → DOM < N элементов | kimi, дипсик | ⏳ Этап 61 |
| 4 | Contract (zod-схемы ответов топ-5 эндпоинтов) + Migration idempotency (прогнать миграции дважды) | kimi, дипсик | ⏳ Этап 60 |

### Coverage-паттерны второй волны (qwen)

- **Клиент**: error boundaries (App.tsx, AdminLayout), offline/network error (api.ts, страницы), двойной submit (Login, TicketCreate, AdminSettings), keyboard navigation (CommandPalette, TicketDetail, DataTable), toast lifecycle (sonner), WebSocket reconnect UI (SocketContext, Tickets, ChatDetail), large data/virtualization (TicketDetail, ChatDetail), form validation edge cases (пробелы, max-length, спецсимволы, XSS-попытка), i18n fallback (неизвестный ключ → key), lazy loading / Suspense fallback.
- **Сервер**: catch-ветки сервисов (мок prisma.$executeRaw → throw), env-условия (NODE_ENV, REDIS_URL есть/нет), migration idempotency, seed validation (FK валидны, нет orphan), full integration flow (supertest create→assign→message→status→close + audit_log + notifications + WS emits), outbox reliability (упал после insert → воркер доставит после рестарта), Redis cache invalidation (PUT /tickets/:id → cache:*:/api/tickets* очищен), BullMQ DLQ (3 фейла → DLQ), rate-limiter edge cases (10-й проходит, 11-й → 429, через минуту снова).

---

## 3. Функционал — остаток

### 3.1 Админка (Этап 58) — ✅ реализована (см. §1)

### 3.2 Resilience (Этап 59) — ✅ закрыт (см. §1)

### 3.3 qwen P0 — отличают корп. Service Desk от «демо» (Этапы 63–64)

| # | Фича | Зачем | Стоимость | Куда |
|---|------|-------|-----------|------|
| 1 | 2FA / TOTP для admin/super_admin (otplib + QR в Profile, require2FA) | без этого не пройдёт аудит безопасности | 2–3 дня | ⏳ Этап 63 |
| 2 | Активные сессии пользователя (view/revoke, таблица sessions) | refresh живёт 7 дней, отозвать нельзя | 1–2 дня | ⏳ Этап 63 |
| 3 | Notification preferences per user (email/push/in-app × события) | сейчас все получают всё | 1–2 дня | ⏳ Этап 63 |
| 4 | Watchers / Subscribers тикета (ticket_watchers + UI) | наблюдение без права редактировать | 1 день | ⏳ Этап 64 |
| 5 | Ticket relations (parent/child, blocked-by, duplicates + UI) | эпик-тикеты и связь дублей | 2 дня | ⏳ Этап 64 |
| 6 | Merge / Duplicate tickets («Объединить с…», перенос сообщений/вложений) | классика Service Desk | 1 день | ⏳ Этап 64 |

### 3.4 qwen P1 — сильно повышает ценность (Этапы 65–66)

| # | Фича | Комментарий |
|---|------|-------------|
| 1 | Версионирование Wiki (wiki_revisions) + diff + rollback | сейчас статья перезаписывается безвозвратно |
| 2 | Редактирование сообщений в чате (edited_at, WS message:edited) | delete есть, edit нет |
| 3 | Reply-to / threads в чате (reply_to_message_id) | цитирование |
| 4 | SLA business hours UI в админке (форма + таймзоны + holidays) | сейчас хардкод Mon–Fri 9–18 в sla.service.js |
| 5 | Custom roles (roles + permissions, конструктор ролей) | снимает боль «нужна ещё одна роль между agent и senior» |
| 6 | Org chart / иерархия (manager_id + граф) | страница «Команда» |
| 7 | Ticket forms / dynamic forms (JSON-схема в ticket_categories) | разные формы для категорий (react-hook-form + zod из схемы) |
| 8 | Scheduled reports (cron + шаблон письма) | еженедельная сводка на email |
| 9 | GDPR data export (JSON/ZIP всех данных пользователя) | кнопка «Скачать мои данные» в Profile |

### 3.5 qwen P2 / nice-to-have — в бэклог

Pinned messages, forward messages, file versioning, shared links (TTL), saved searches/facets, Slack/Discord/Jira интеграции, CalDAV sync; voice/video (WebRTC) и mobile app (React Native) — **вне скоупа** корп. интранета.

---

## 4. Новое ПОВЕРХ плана 54–66 — «стоит добавить» (qwen + дипсик 24.09)

> Это пункты, которых ещё **нет** в плане 54–66 («Что-доделать.txt» раздел «Дополнения из аудитов 24.09»). Дипсик помечает их ⚡, qwen — «новое». Приоритеты — из аудитов.

### P1 — прод-риски / большие дыры (дипсик)

| # | Фича | Суть | Эндпоинты / таблицы |
|---|------|------|----------------------|
| 1 | **Approval chains** | requester → manager → director → resolved; «Запросить approval» (requester), «Approve/Reject» (senior_agent+) | POST /tickets/:id/approve\|reject, GET /tickets/:id/approvals; ticket_approvals (ticket_id, requester_id, approver_id, status, comment, decided_at) |
| 2 | **Team/Group scope** | сейчас assign только на человека; тикет на команду, доступ по членству | teams + team_members; GET/POST /admin/teams + members; tickets.team_id |
| 3 | **Field-level permissions** | скрытие полей (time_spent, assignee для requester, «Стоимость» только senior_agent+) | field_permissions (role_id, field_name, access_level read/write); GET/PUT /admin/field-permissions |
| 4 | **SLA policies per category/priority** | сейчас один глобальный SLA; гибкие политики + несколько бизнес-календарей | sla_policies + business_hours_calendars; CRUD /admin/sla-policies; tickets.sla_policy_id |
| 5 | **Webhooks journal + replay** | retry есть, видимости доставок нет | GET /admin/webhooks/:id/deliveries, POST /replay |
| 6 | **Impersonation для super_admin + аудит** | воспроизвести баг пользователя | POST /admin/impersonate/:userId + impersonation log |

### P2 / бэклог — полезно, дешево

| # | Фича | Стоимость |
|---|------|-----------|
| 1 | Отчёты: Agent performance (FRT/TTR/CSAT по исполнителю), Backlog aging (старше N дней), SLA compliance % по категориям | 2–3 дня |
| 2 | KB-статья из решённого тикета («Создать статью») | 1 день |
| 3 | Auto-close chain (resolved → N дней без ответа → closed + CSAT) | 1 день |
| 4 | Duplicate detection при создании (похожие тикеты → предложить merge) | 1–2 дня |
| 5 | Canned responses: категории/теги + массовое редактирование + переменные ({{ticketId}}) | 1–2 дня |
| 6 | Ticket templates (шаблоны тикетов с полями/custom fields, не только email) | 2 дня |
| 7 | Onboarding/offboarding chains (welcome email + transfer tickets + revoke sessions) | 2–3 дня |
| 8 | Renewal chain (SLA скоро истекает → превентивное уведомление) | — |
| 9 | Escalation chain v2 (уведомить watchers + manager_id) | — |
| 10 | Auto-assign v2 (по team skills/category/нагрузке, а не «наименее загруженный») | — |
| 11 | Custom ticket statuses + transitions (ticket_statuses — замена хардкода VALID_TRANSITIONS) | — |
| 12 | Scoped API-токены: scopes в модели есть, но не enforced по роутам → UI + enforcement | — |
| 13 | Cache flush (Redis по префиксу), Migrations status UI, Backup/restore UI (список + «восстановить») | — |
| 14 | Email-шаблоны: версионирование + diff | — |
| 15 | Rules / Settings export-import (JSON) | — |
| 16 | Preview-as-role («Просмотр как requester») | 1 день |
| 17 | «Клонировать тикет» (копия + связь duplicate) / «Эскалировать вручную» / «Пауза SLA» (sla_pauses: paused_at/resumed_at/reason + пересчёт due_at) | qwen-кнопки |
| 18 | Kanban: WIP limits, drag-n-drop reorder (LexoRank + WS), real-time cursors (WS ticket:viewing) | qwen-UI |
| 19 | Счётчики/фавориты: «В избранное» (list_favorites), pin message (chat_pinned_messages), forward message, share link TTL (presigned URL), экспорт в Jira | qwen-кнопки |
| 20 | tickets.source (enum email/portal/api/phone/chat/manual) под аналитику; priority_weight для автоназначения | qwen-БД |

---

## 5. E2E — дыры (слоты по цепочкам)

| # | Спека | Что проверяет | Приоритет | Источник |
|---|-------|---------------|-----------|----------|
| 1 | admin-chains.spec.ts | проход по всем разделам админки, клик по каждой Primary-кнопке → assert на запрос (page.waitForRequest) | P1 | kimi |
| 2 | admin-users.spec.ts | создание пользователя + роль + блокировка | P0 | qwen |
| 3 | admin-rules.spec.ts | создание правила автоматизации + срабатывание | P0 | qwen |
| 4 | ticket-attachments.spec.ts | lifecycle тикета с вложениями | P0 | qwen |
| 5 | chat-read-receipts.spec.ts | 2 пользователя, сообщения + read receipts | P1 | qwen |
| 6 | wiki-lifecycle.spec.ts | создание статьи + поиск | P1 | qwen |
| 7 | calendar-lifecycle.spec.ts | создание события + проверка на дашборде | P1 | qwen |
| 8 | mobile-navigation.spec.ts | viewport 375px | P1 | qwen |
| 9 | offline-queue.spec.ts | сообщение после реконнекта | P1 | qwen |
| 10 | error-boundary.spec.ts | сломанный компонент → fallback | P2 | qwen |
| 11 | a11y-ticket.spec.ts | tab через всю форму тикета | P2 | qwen |
| 12 | export-real / file-upload-real / search-real | assert на данные (см. §2 P1) | P1 | kimi, дипсик |

После этого E2E станет ~25+ реальных сценариев вместо smoke. (kimi)

---

## 6. Роуты / цепочки / меню / кнопки — инвентарь (дипсик §4)

Полного инвентаря роутов, меню и кнопок в репозитории нет — для аудита «приложение → админка → БД → роуты → цепочки → меню → кнопки» не хватает:

| Что | Файл |
|-----|------|
| Инвентарь роутов (сверить router.METHOD с Swagger и api.test.js) | ⚠️ нет в репо |
| Меню по ролям (сверить с requireRole на бэке) | src/components/Sidebar.tsx, src/layouts/AppLayout.tsx, src/layouts/AdminLayout.tsx |
| Кнопки (UI-инвентарь: data-testid, роуты, disabled по роли) | src/pages/**/*.tsx |
| Цепочки workflow | tickets.js VALID_TRANSITIONS |
| Цепочки rules engine (9 операторов × 5 действий) | rules.service.js evaluateRules + executeActions |
| Цепочки recurrence | recurrence.service.js processRecurrences (cron-parser v5) |
| Цепочки SLA-эскалации (интервалы, дедуп) | background.js runSlaCheck |
| Цепочки outbox (delivered/retry/DLQ) | event_outbox + outbox-worker.js |
| Меню админки (куда встроены 11 фич Этапа 58) | AdminLayout.tsx |
| Меню мобильное (синхронизация с desktop) | MobileNav |

Рекомендация дипсик: один пункт меню/одна цепочка → check-console.mjs + E2E-спека на неё → зафиксировать.

---

## 7. БД — что проверить фактически (дипсик §5)

- 32 модели и индексы (сверить schema.prisma с seed.sql) — актуально
- Каскады FK (onDelete) — ✅ **проверено и применено в Этапе 59**: идемпотентная миграция `20260924_fk_cascades.js`, 32 FK (created_by → RESTRICT, assigned_to/creator_id → SET NULL, остальные CASCADE), явные onDelete в schema.prisma
- Idempotency keys — ✅ вне Redis (in-memory fallback, TTL 24ч, timeout 500мс) — Этап 59
- Транзакции multi-step — ✅ bulk (batch $transaction), assign/status/priority (interactive) — Этап 59
- FULLTEXT-индексы (server/migrations/20260710_*), ANALYZE TABLE daily для FULLTEXT — проверить
- Soft-delete целостность — ✅ `deleted_at: null` во всех services (Этап 57)
- Backup verification — ✅ (Этап 53)
- Migration idempotency — нет теста (прогнать миграции дважды) → Этап 60
- knex migrate:latest в CI — сейчас только mysql < seed.sql; новые knex-миграции применять вручную (temp-скрипт)

---

## 8. Инфраструктура — включить в re-audit (дипсик §6)

vite.config.ts, playwright.config.ts, eslint.config.js, server/vitest.config.js (причина прошлых падений 86 тестов), .env.example (обязательные/опциональные), nginx/default.conf, k8s/*.yaml, check-console.mjs, .husky/pre-commit, docker-compose.override.yml — проверить все файлы в финальном re-audit свежей папкой test-analysis.

---

## 9. Процесс / правила

| # | Правило | Источник | Статус |
|---|---------|----------|--------|
| 1 | Правило порогов в AGENTS.md: буфер +2 п.п. vs обязательные тесты (сейчас буфер 0.2–0.7 п.п. — CI-хрупко) | дипсик, kimi, qwen | ⏳ Этап 60 |
| 2 | CI-check синхронности «Что-доделать ↔ test-inventory» (иначе каждый AI-аудит даст разброс цифр) | дипсик | ⏳ Этап 60 |
| 3 | «фича = флаг» — новый публичный модуль без useFeature() не мержится | уже в AGENTS.md (правило 12) | ✅ |
| 4 | ESLint 57 warnings → <20 (постепенно, no-explicit-any как error) | qwen, дипсик | ⏳ |
| 5 | Dockerize dev (docker-compose.dev.yml с MySQL + Redis) | qwen | ⏳ |

---

## 10. Рекомендуемый порядок (после закрытия Этапа 59)

| Этап | Состав | Итог |
|------|--------|------|
| ~~54–58~~ | ~~база, админка~~ | ✅ закрыты |
| ~~59~~ | ~~Resilience: JWT rotation, idempotency вне Redis, транзакции, FK каскады, SLA/TZ, refresh family, trust proxy + oidc/background sub~~ | ✅ **закрыт 24.09** (1451 тест) |
| 60 | Процесс: правило порогов в AGENTS.md, финальный re-audit свежей папкой test-analysis, **i18n parity, contract (zod), migration idempotency** | доверие к цифрам |
| 61 | Покрытие ядра: ticket-context.optimistic, TicketDetail/ChatDetail/Kanban, virtual scroll jsdom, E2E export/upload/search-real, **XSS-инвариант**, notify регресс-тест, guard-тесты (limit/soft-delete/N+1) | цепочки UI и E2E-реализм |
| 62 | Timeline + импорт: GET /tickets/:id/history + вкладка «История» + PDF; CSV-импорт сотрудников UI; retention-политика | «Оценка ии.rtf» №8–11 |
| 63 | qwen P0 #1–3: 2FA/TOTP, активные сессии, notification preferences | ~40 серв / ~25 клиент / ~3 E2E |
| 64 | qwen P0 #4–6: watchers, ticket relations, merge | ~30 серв / ~20 клиент / ~2 E2E |
| 65 | qwen P1 ч.1: wiki versioning, chat edit/reply, SLA business hours UI | ~35 серв / ~25 клиент / ~3 E2E |
| 66 | qwen P1 ч.2 + финальный аудит: custom roles, org chart, ticket forms (+ при желании scheduled reports, GDPR); **coverage push: client branches 65+, server 70+, E2E ~80 (~1780 тестов)**, инвентарь и docs перегенерированы | ~160 тестов |

**Итог в цифрах** (qwen): сейчас 1451 → после Этапа 66 ~1780 тестов; client branches 61.21 → 65+, server branches 64.64 → 70+; E2E 57 → ~80.

Сверх плана — раздел §4 (approvals, teams, field-level, SLA-политики, webhooks journal, impersonation, custom statuses, отчёты…) — складывать в отдельный бэклог или новый Этап 67 после согласования.

---

## 11. Что НЕ делать (свод антипаттернов)

- Snapshot-тесты (хрупкие), visual regression, mutation testing, read replica
- Тесты на shadcn/ui примитивы (Button, Input) — покрытие чужого кода
- Покрытие констант/типов/интерфейсов; coverage до 90% (закон убывающей доходности)
- Tauri / mobile app (удалено осознанно); voice/video, Slack/Teams/Jira — вне скоупа (бэклог)
- Промты «оцени проект целиком» пачкой; похвала без причины и «стоит рассмотреть» — только «сделай так»
- Промты, запрещённые в playbook: «генерируй и делай что хочешь», «обобщи/упрости/переведи» без файлов, «предложи идеи» (без запроса)
- (kimi, qwen, дипсик)

---

## 12. Ключевые вердикты по вопросу «достаточно ли для полного функционала?»

| Критерий | qwen | дипсик |
|----------|------|--------|
| Корп. Service Desk (уровень Zendesk/Freshservice) | ✅ да, 80–85% use cases | ✅ да, ~90–95% после плана 59–66 + добавки §4 |
| Полный ITIL (ServiceNow/BMC) | ❌ нет (CMDB + Change/Problem Management, ~6–12 недель) | ❌ нет, ~50–60% |
| Корпоративный интранет (внутренняя поддержка) | ✅ да, с избытком | ✅ да |
| SaaS-продукт (продажа клиентам) | ⚠️ частично (нужен multi-tenancy + compliance) | ⚠️ частично |
| Что важнее: ещё 20 фич или глубина (depth) | — | **Depth**: 2FA, field-perms, SLA-фикс, e2e-realism, coverage core — «работает в проде», а не «работает на демо» |

**Вне скоупа (осознанно)**: voice/video (WebRTC), mobile app (React Native), billing/invoicing, dispatch, нативный desktop, read replica, visual regression.
**Дорого и часто нужно (ITSM, отдельный класс)**: Problem Management (12 нед), Change Management (2–3 нед), Asset/CMDB (3–4 нед), Service Catalog (2 нед поверх form builder), Report builder (2–3 нед), Visual workflow builder (1–2 нед), Multi-tenancy (4+ нед), Slack/Teams/Jira (по 1 нед).

**Главное (дипсик)**: «полный функционал» ≠ максимум фич. Есть разрыв по breadth (фичи) и по depth (надёжность/тесты). Правильный порядок: сначала P0 глубины (Этап 59 ✅ + Этап 61), затем 63–64 (2FA, watchers, relations — реально нужны в проде), затем 62, 65, 66 + добавки §4. Классический Service Desk будет закрыт ~90–95%.

---

## 13. Дополнения разработчика (24.09) — верификация «Оценка ии.rtf» по коду

> Внешний ИИ оценил раздел «что бы я добавил» в **8.5/10** и **10/12** находок, но потребовал проверить каждый пункт по коду (его вердикт о «дырявом regex-экранировании XSS» и «отсутствии reuse-detection» по коду НЕ подтвердился). Проверено вручную по коду 24.09.

### Тесты

| # | Пункт | Вердикт по коду 24.09 | Приоритет | Куда |
|---|-------|----------------------|-----------|------|
| 1 | XSS в client-рендере (MarkdownEditor.tsx:54, TicketDetail.tsx:570 — dangerouslySetInnerHTML) | Экранирование `& < >` выполняется ПЕРВЫМ → raw-теги (`<img onerror>`, `<svg onload>`, `javascript:`) не исполняются. Риск ниже заявленного. Нужен regression-тест на инвариант; при расширении markdown (ссылки/таблицы) — заменить на DOMPurify/react-markdown | P1 | Этап 61 |
| 2 | Refresh-token reuse-detection (auth.js) | БАЗОВЫЙ механизм ЕСТЬ (старый токен удаляется при каждом /refresh, auth.js:81). Отсутствовали token_family_id и отзыв ВСЕХ сессий при reuse | P1 | ✅ **Этап 59** (`1811fee`): family_id + reuse-detection отзывает всю семью, POST /auth/revoke-all, +5 тестов |
| 3 | trust proxy: 1 + X-Forwarded-For (app.js:68) | Сначала проверить топологию (compose 1 nginx — 1 корректен vs k8s ingress+svc — другой); тест «спуфинг XFF не обходит лимит» + WS-handshake-лимитер | P1 | ✅ **Этап 59**: trust-proxy.js (TRUST_PROXY=1\|true → 1, off по умолчанию), спуфинг-тесты + тесты WS |
| 4 | SLA / бизнес-часы TZ + DST (sla.js date.getDay()) | 🐞 **БАГ ПРОДУКТА ПОДТВЕРЖДЁН**: TIMEZONE из admin_settings не используется в addBusinessHours → дедлайн клиента в другом TZ считается неверно; DST ±1ч | P0 | ✅ **Этап 59** (`af06219`): TIMEZONE в addBusinessHours/getRemainingBusinessMs/isWithinBusinessHours (Intl.DateTimeFormat, DST), +7 тестов sla.timezone |
| 5 | Инъекция через orderBy/limit | УЖЕ ЗАЩИЩЕНО: orderBy захардкожен (tickets.service.js:88), limit клампится Math.min(100/200/500) во всех list-роутах. «limit=999999 = DoS» неприменимо → тест-предохранитель | P2 | Этап 61 |
| 6 | Уведомления не роняют основную операцию | safeNotify есть (Этап 17), интеграционного теста «email/webhook упал → тикет создан, 201» нет — классический регресс-риск | P1 | Этап 61 |
| 7 | CSP-эффективность (helmet) | CSP настроен (app.js:84-85, connect-src localhost:*); теста «inline <script> реально блокируется» нет. В связке с №1 | P2 | Этап 60 |
| 8 | Idempotency-Key: коллизия | Семантика корректна: тот же ключ → тот же кешированный ответ (middleware/idempotency.js). Разный payload с тем же ключом не даёт 409 (by design) — зафиксировать тестом | P2 | Этап 60 |
| 9 | Max body size (express.json 100kb по умолчанию) | PUT /admin/settings с большим JSON → 413 (не 500) — проверить тестом; multer 50MB отдельно | P2 | Этап 60 |
| 10 | Soft-delete в списках | УЖЕ покрыто: deleted_at: null во всех services (tickets/chats/files/api-tokens/recurrence/collision/email-ingestion). Тест на list-эндпоинты — предохранитель | P2 | Этап 61 |
| 11 | N+1 предохранитель | Фикс Этапа 57 без теста «≤ N запросов к БД» (spy на prisma) — фиксирует, что фикс не откатился | P2 | Этап 61 |

### Функционал

| # | Пункт | Вердикт по коду 24.09 | Приоритет | Куда |
|---|-------|----------------------|-----------|------|
| 1 | Timeline изменений в карточке тикета | ПОДТВЕРЖДЕНА ДЫРА: audit_log пишется в 12 роутерах (audit.js + middleware), UI нет (grep TicketDetail = 0). GET /tickets/:id/history + вкладка «История» → дальше попадёт в PDF-экспорт | P1 | Этап 62 |
| 2 | Ответ письмом → сообщение в тикете | ФИЧА УЖЕ ЕСТЬ: email-ingestion.service.js:100-111 — `[Ticket #N]` в subject/reply → сообщение в существующий тикет (28 тестов). Недостаёт E2E-теста и доки, не фичи | P2 | Тест + дока |
| 3 | CSV-импорт сотрудников | Эндпоинт ЕСТЬ: admin.js:446 POST /employees/import (фикс bcrypt — Этап 47). Проверить UI-кнопку, валидацию, тест роута | P2 | Этап 62 |
| 4 | Retention / автоочистка | Уведомления чистятся >90д (background.js); audit_log/files/chat_messages — НЕ чистятся. Сначала политика хранения (GDPR/152-ФЗ), потом cron-задача | P2 | Инфра / Этап 62 |
| 5 | Экспорт PDF с историей | Связано с №1: как только timeline появится в карточке — jsPDF его подхватит | P2 | слить с №1 |
| 6 | KB-статья из тикета | Ассистент ищет Wiki; кнопки «привязать KB-статью» в TicketDetail нет — проверить необходимость (фичи нет, решение по продукту) | P2 | проверить |

### Процесс

| # | Пункт | Вердикт | Приоритет |
|---|-------|---------|-----------|
| 1 | DoD на фичу: флаг + тест + i18n + check-console + инвентарь | AGENTS.md сейчас содержит только «фича = флаг» — дописать остальные 4 пункта | P2 |
| 2 | История coverage между прогонами (coverage-history/<date>.json + warning при регрессе >0.5 п.п.) | требует CI-инфраструктуры | P2 |
| 3 | Верификация AI-пункта по коду перед внесением в план | правило «AI-пункт без цитаты кода не идёт в "Что-доделать"» — в AGENTS.md (Kimi/DeepSeek давали ложные P0) | P1 |
| 4 | «Сначала фикс, потом тест» — не закреплять тестом дырявую реализацию | в AGENTS.md | P2 |