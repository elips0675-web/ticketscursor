Ваша прошлая проблема: AI-аудиторы галлюцинировали (несуществующие тесты, LaunchDarkly, felizCumpleaños), потому что им скармливали краткие выжимки вместо репозитория. Поэтому первый промт — мета-правило, остальные — под конкретные задачи из моей оценки.
0. Мета-промт (приклеивать к любому аудиту)
plain
Ты — senior code reviewer. Правила:
1. НЕ выдумывай файлы, тесты, функции. Каждое утверждение подкрепляй
   цитатой из реального кода, который ты прочитал.
2. Если файла нет в репозитории — скажи «нет в репо», не додумывай.
3. В конце отчёта: список «Проверено» (файлы, которые реально читал)
   и «Не проверено» (не хватило контекста).
4. Запрещены оценки вида «выглядит хорошо» — только факты с доказательствами.
🔴 Сначала починить доверие
1. Верификация CI (главный)
plain
Прочитай .github/workflows/ci.yml. Для КАЖДОГО job'а: какие шаги,
какие зависимости ставятся, что реально выполнится на ubuntu-latest.
Найди шаги, которые гарантированно упадут (cmd /c, отсутствующие
npm ci, гонки серверов без wait-on). Дай исправленную версию файла.
2. Docker-деплой
plain
Проанализируй Dockerfile (multi-stage) и docker-compose.yml.
Проверь: какая стадия используется при build без target, совпадает
ли она с CMD сервисов в compose, используется ли entrypoint.sh.
Дай минимальный фикс: compose собирает рабочий бэкенд + фронт.
3. Истина по тестам (без AI-инвентарей)
plain
Напиши скрипт scripts/test-inventory.mjs: рекурсивно находит
*.test.{ts,tsx,js}, парсит describe/it/test, выводит JSON:
{ file, suiteCount, testCount }. Запусти его. Результат — в README
вместо текущих цифр. AI-оценки количества тестов запрещены.
4. Разбор конфликта api.test.js
plain
Прочитай server/src/api.test.js и vitest.global-setup.js. Запусти
только POST /api/tickets тесты. Ответь одно из двух: (а) падает —
вот причина и фикс, (б) проходит — значит Что-доделать.txt устарел.
🟡 Качество
5. Удаление Knex / двойной валидации
plain
Найди все импорты knex в server/src (кроме миграций). Для каждого
файла: перепиши запрос на Prisma, сохранив семантику. express-validator:
найди все использования, если вся валидация на Zod — удали пакет.
Порог: `npm test` в server зелёный после каждого изменения.
6. Coverage-рачётка
plain
Прочитай vitest.client.config.ts и последний coverage-отчёт.
Предложи новые thresholds = min(текущее + 2, 80) по каждой метрике.
Список 5 файлов с худшим покрытием — что реально поднять за день.
7. Нормальные нагрузочные тесты
plain
Перепиши test/load/tickets.k6.js: аутентификация один раз в setup(),
не в каждой итерации. Добавь сценарий: создание тикета (POST) с
проверкой 201, поиск (GET /api/search), и сценарий на чтение
комментариев. Thresholds оставь. Убери dev-login из default-функции.
8. Глубокие E2E
plain
Напиши e2e/ticket-lifecycle.spec.ts: создать тикет → назначить
агента → сменить статус open→in_progress→resolved → проверить,
что изменения отображаются в списке и на детальной странице.
Ассерты на данные, не на наличие h1. Базовый URL и auth из
storageState, как в существующих спеках.
🟢 Безопасность и рост
9. Security-аудит (короткий, но жёсткий)
plain
Проверь: (1) dev-login отключён при NODE_ENV=production,
(2) нет секретов в коде/репо (grep по JWT_SECRET, пароли, токены),
(3) права на все DELETE/PATCH роуты (может ли requester удалить чужое),
(4) загрузка файлов: MIME + magic bytes + лимит размера.
Формат: находка → файл:строка → критичность → фикс.
10. N+1 и производительность Prisma
plain
Пройди по server/src/routes и services. Найди запросы в циклах,
отсутствующие include/select, FULLTEXT без индексов, list-эндпоинты
без пагинации. Топ-5 по влиянию на прод с фиксами.
11. Пакеты
plain
Проанализируй package.json (root + server): prisma в devDependencies?,
@types-пакеты в dependencies?, пакеты, не импортируемые нигде (depcheck),
major-версии с уязвимостями. Дай список «убрать/перенести/обновить».
Главный совет
Не запускайте эти промты пачкой. Один промт → одно изменение → npm test + check-console → коммит. Это прямо ваше же правило №3 из AGENTS.md («не ломать работающее») — просто AI тоже должен его соблюдать.
С чего начать: промт 1 (CI), потому что он снимает вопрос «а зелёный ли у меня на самом деле билд» — без этого остальное опирается на песок.




Каких файлов не хватает для качественного AI-аудита
Сейчас у тебя в test-analysis/ — «обёртка» проекта: тесты, инфра, конфиги, документы. Но самого кода там нет. Поэтому AI-аудиты (Kimi, Qwen, DeepSeek) дают оценки «по описанию», а не по факту — отсюда и разброс 6.5 / 8.5 / 7.

Вот что реально нужно докинуть.

🔴 Критично (без них аудит — гадание)
1. prisma/schema.prisma
Без него никто не поймёт:

связи моделей,

индексы,

каскады,

nullable-поля,

enum’ы.

Это самый важный файл для анализа бэкенда. 25 моделей — это не по описанию оценишь.

2. server/src/app.js + server/src/middleware.js
Точка сборки всего API. Здесь видно:

порядок middleware (auth → rate-limit → routes),

как монтируются роутеры,

API versioning (/api/v1/),

CORS, helmet, CSP,

error handler.

Без этого нельзя оценить безопасность.

3. server/src/routes/tickets.js + server/src/services/tickets.service.js
Самый большой кусок бизнес-логики. Именно тут AI найдёт:

N+1,

гонки,

некорректные транзакции,

утечки прав.

4. server/src/routes/auth.js + server/src/auth/oidc.js
Auth — самое опасное место. Оценивать без кода = пальцем в небо.

5. src/lib/api.ts + src/context/AuthContext.tsx + src/context/ticket-context.tsx
Фронтовое ядро. Здесь:

обработка ошибок,

refresh-токены,

optimistic updates,

race-условия.

6. vite.config.ts + playwright.config.ts + eslint.config.js
Их нет — а именно там видно, почему:

vitest не подхватывает jsdom,

bundle-check не работает,

E2E — smoke-only.

🟡 Важно (для глубокого разбора)
Файл	Зачем
.env.example	Какие переменные обязательны, какие опциональны
server/src/background.js	BullMQ + DLQ + IMAP polling — сложная логика
server/src/notify.js	Единый узел уведомлений (in-app/email/telegram/push)
server/src/socket.js	WebSocket + комнаты + rate-limit
server/src/lib/cache.js	Redis с Lua-инвалидацией
server/vitest.config.js (или его отсутствие)	Причина падения 86 тестов
server/src/migrations/*.js (5–10 последних)	Реальный DDL, не Prisma-абстракция
src/pages/TicketDetail.tsx	Самый большой фронт-компонент
src/pages/AdminSettings.tsx	Весь админ-функционал в одном файле
src/i18n/ru.json (размер + структура)	Проверить, нет ли опять дублей ключей
🟢 Опционально (для «продвинутого» аудита)
nginx/default.conf — уже есть, ок

k8s/*.yaml — если хочешь ревью оркестрации

docker-compose.override.yml (если есть)

check-console.mjs — понять, что именно он проверяет

husky/pre-commit — качество pre-commit хука

tailwind.config (если есть)

Tauri-конфиги — если ещё актуальны

📋 Готовый промт для AI-аудита
Если хочешь получить осмысленный разбор, а не «оценку по README», давай такой промт:

text
Ты — senior fullstack-ревьюер. Ниже — реальная кодовая база Service Desk 
(React 19 + Express 5 + Prisma + MySQL + Socket.IO).

ЗАДАЧА: проведи аудит по 6 осям. По каждой — конкретные findings с 
цитатой кода и номером строки, не общие слова.

1. БЕЗОПАСНОСТЬ: аутентификация, RBAC, инъекции, утечки, SSRF, 
   раскрытие ошибок, CORS/CSP, rate-limit.
2. КОРРЕКТНОСТЬ: race conditions, транзакции, N+1, необработанные 
   промисы, forgotten await, fallback'и на catch.
3. АРХИТЕКТУРА: слоистость, дублирование, циклические зависимости, 
   смешение ответственностей, мёртвый код.
4. ПРОИЗВОДИТЕЛЬНОСТЬ: запросы к БД, кэш, индексы, размер бандла, 
   virtual-рендер.
5. ТЕСТЫ: что реально проверяется, что smoke-only, что мокается так, 
   что не имеет смысла.
6. ИНФРА: Docker, CI, миграции, секреты, health-check, graceful 
   shutdown.

ФОРМАТ ОТВЕТА:
- Таблица: severity | файл:строка | проблема | фикс (1 строка).
- Топ-5 самых критичных — разобрать подробно с патчем кода.
- Что НЕ проблема, но выглядит как проблема — отдельным списком 
  (чтобы не тратить моё время).

НЕЛЬЗЯ:
- Хвалить без причины.
- Писать «стоит рассмотреть» — только «сделай так».
- Оценивать файлы, которых нет в контексте.

Игнорируй README.md, CHANGELOG.md и context.txt как источник правды — 
источник правды только код.
🎯 Что реально стоит делать
Сейчас у тебя есть 2 варианта:

Вариант A — «лёгкий» (для быстрых проверок)
Оставляешь как есть, но добавляешь в папку:

prisma/schema.prisma

server/src/app.js

server/src/routes/tickets.js

src/lib/api.ts

Это ~200 КБ. Хватит, чтобы AI понял «как оно на самом деле устроено».

Вариант B — «полный» (для настоящего ревью)
Собираешь отдельный архив:

bash
# Только код, без node_modules и dist
git archive --format=zip -o audit.zip HEAD \
  -- 'server/src/**' 'src/**' 'prisma/**' '*.config.*' '*.json'
~2–3 МБ. Даёшь AI вместе с текущим test-analysis/. Тогда аудит будет по делу.

Про сами промты (если ты это имел в виду)
Если вопрос был «какие промты использовать в работе», то вот минимальный набор, который реально экономит время:

Промт	Когда
«Найди баги, не рефактори»	Перед релизом
«Отрефактори X под Y, не меняй поведение»	Техдолг
«Объясни, что делает этот файл, по шагам»	Онбординг в свой же код через месяц
«Напиши тест на X, покрой edge-cases: null, пустой массив, 401, 500»	Всегда
«Найди утечки памяти в этом компоненте»	Frontend
«Проверь SQL на N+1 и предложи eager-loading»	После новых эндпоинтов
«Сделай code review как строгий тимлид, можно материться»	Раз в неделю
И главное правило: не проси оценить проект целиком — проси конкретный файл и конкретную ось (безопасность / производительность / корректность). Тогда ответ будет полезным, а не «7/10, есть куда расти».

TL;DR: докинь schema.prisma, app.js, tickets.service.js, api.ts и vite.config.ts — и любой AI-аудит сразу станет в 3 раза точнее. Без кода он просто пересказывает твой же README.



AI в разработке — это не только "проверь мой код". Это полноценный набор ролей, которые закрывают 80% рутины программиста. Вот готовый набор промптов, адаптированный под ваш проект (Service Desk на React + Node.js + Prisma + Playwright).
🎭 12 ролей AI для программиста
1. 🔍 Code Reviewer (Ревьюер кода)

1234567891011
2. 🏗️ Архитектор (Проектирование фич)

123456789
3. 🐛 Дебаггер (Поиск багов)

1234567891011
4. 🧪 Генератор тестов

123456789
5. 📝 Документатор

123456789
6. ⚡ Оптимизатор производительности

12345678
7. 🔐 Security Auditor (Аудит безопасности)

123456789101112
8. 🗄️ DB Designer (Проектирование БД)

123456789
9. 🧰 Рефакторщик

12345678910
10. 📚 Объяснятор (Обучение)

12345678
11. 🔄 Мигратор / Upgrader

12345678
12. 🎭 Генератор моков и тестовых данных

1234567
🧠 Мета-промпты (универсальные усилители)
Добавляйте их в конец любого запроса, чтобы получить ответ на 30% качественнее:

1

12

1

1

1
📋 Шпаргалка: когда какой промпт использовать
Ситуация
Роль AI
Написал фичу, боюсь что-то упустить
Code Reviewer
Нужно спроектировать новое
Архитектор
Тесты падают / ошибка в проде
Дебаггер
Покрыл код тестами на 40%
Генератор тестов
Коллега не поймёт мой код
Документатор
Страница грузится 3 секунды
Оптимизатор
Релизим в прод
Security Auditor
Нужна новая фича в БД
DB Designer
Код работает, но выглядит уродливо
Рефакторщик
Не понимаю, как это работает
Объяснятор
Обновляем зависимости / стек
Мигратор
Нужны данные для демо
Генератор моков
Главный совет: AI — это не волшебник, а стажёр с энциклопедическими знаниями. Чем точнее вы опишете контекст (стек, ограничения, цель), тем полезнее будет ответ. Всегда прикладывайте:
❌ "Сделай мне компонент"
✅ "React-компонент списка тикетов с пагинацией, фильтрами по статусу и поиском. Использовать TanStack Query, Tailwind, data-testid для тестов. Максимум 200 строк."
Хотите, я покажу, как использовать эти промпты на конкретной задаче из вашего Service Desk? Например, спроектировать SLA-уведомления или написать тесты на tickets.spec.ts?

---

## Промты из финальных оценок 23.09 (Kimi 8/10, DeepSeek 8.5/10, Qwen 8.5/10)

> Источник: «Оценка kimi/дипсик/qwen.rtf». Сверено с инвентарём 1298 тестов — почти всё из их списков уже закрыто Этапами 46–56,
> поэтому промты ниже — только на РЕАЛЬНЫЙ остаток (он же — Этапы 57–61 в корневом «Что-доделать.txt»).
> Формат: один промт → одно изменение → `npm test` + `check-console.mjs` → коммит. Каждый промт < 1500 токенов.

### A. Race-тесты (Дипсик P1, Qwen P1) — Этап 57
plain
Напиши тесты на гонки для тикетов в server/src/__tests__/:
1. ticket-lock.race.test.js — два параллельных POST /:id/lock против реального API: один → 200, второй → 409;
   суперстарые данные подготовь в БД (vitest.global-setup.js создаёт servicedesk_test).
2. ticket-status.race.test.js — два параллельных PUT /:id/status (open→resolved): resolved_at записан один раз.
3. outbox.delivery.test.js — event_outbox → воркер доставил → delivered; сбой доставки → retry/DLQ.
Используй супертест/реальную БД как в socket.test.js (порт 0). Перед гонками — сброс таблиц.

### B. auth/oidc.test.js (Дипсик P1) — Этап 59
plain
Напиши backend-тесты на server/src/auth/oidc.js (единственный сервис Этапа 34 без тестов):
discovery fetch, построение authorization URL, callback exchange (обмен code → token),
auto-provisioning нового employee (role=requester), discovery 500 → 502/500, невалидный code → 400.
Мокай fetch (discovery/token/userinfo). Не трогай SSO.test.tsx (клиентский, уже есть).

### C. ticket-context.optimistic.test.tsx (Дипсик/Kimi/Qwen P1) — Этап 61
plain
Подними coverage src/context/ticket-context.tsx (сейчас 35%):
1. createTicket — optimistic insert: тикет появился сразу; API 500 → rollback, тикет исчез, toast.
2. updateStatus — optimistic смена статуса; API 403 → rollback на старый.
3. после любой мутации — invalidateQueries(['tickets']) вызван.
Mock api.ts, React Query через QueryClient (как в Tickets.expanded.test.tsx).

### D. E2E-реализм (Дипсик P1) — Этап 61
plain
Допиши 3 спека в e2e/ (ассерты на данные, не на наличие элемента):
1. export-real.spec.ts — клик «Экспорт CSV» → page.waitForEvent('download') → файл непустой, содержит BOM (\uFEFF).
2. file-upload-real.spec.ts — setInputFiles({ name: 'test.txt', mimeType: 'text/plain', buffer }) → файл в списке →
   клик → скачать → содержимое совпадает.
3. search-real.spec.ts — Ctrl+K → ввод «тикет» → в результатах есть title тикета из seed (не просто «инпут виден»).

### E. i18n parity + XSS/injection (Kimi/Дипсик P1) — Этап 60
plain
1. src/i18n/parity.test.ts — читает ru.json и en.json: набор ключей идентичен в обе стороны (рел. путь к файлам через import.meta.url).
2. server/src/__tests__/injection.test.js — XSS '<script>alert(1)</script>' в title тикета: сохранён как строка (экранирование на рендере, API не хранит HTML-исполнение); SQL-инъекция в query параметры search → 400/экранировано, не 500 и не падение БД.

### F. a11y (Дипсик P2, Qwen) — Этап 58/61
plain
Добавь a11y-тесты (jest-axe или ручные проверки) на ключевые страницы:
AdminSettings/AdminLayout (Этап 58) + Sidebar/Dialog:
icon-only кнопки имеют aria-label, формы — htmlFor+id, Tab-навигация по Sidebar, Esc закрывает Sheet, focus-trap в Radix Dialog.

### G. Background sub-тесты (Дипсик P2) — Этап 59
plain
Раздели background.expanded.test.js (10 тестов) на sub-кейсы:
runSlaCheck с эскалацией по интервалам, cleanup уведомлений >90 дней (свежие не тронуты),
DLQ >10 за час → email админам, stopImapPolling без запуска не падает (идемпотентность).

### H. Contract + миграции (Дипсик P2) — Этап 60
plain
1. contract.test.js — для топ-5 эндпоинтов (GET /api/tickets, GET /:id, POST, PUT /status, POST /:id/messages):
   ответ проходит Zod-схему из server/src/schemas.js (переиспользуй схемы, не дублируй).
2. migration.idempotent.test.js — прогони knex migrate:latest дважды против servicedesk_test: вторая итерация не падает и ничего не меняет.
   (knex-migrate уже идемпотентен по истории, но тест зафиксирует регресс.)

### Список файлов для Этапа 60 (re-аудит) — из Промты.txt + Киmi
plain
Собери свежую папку test-analysis/ для финального re-аудита, добавив к инвентарям КОД:
prisma/schema.prisma, server/src/app.js, server/src/routes/tickets.js, server/src/services/tickets.service.js,
src/lib/api.ts, src/context/ticket-context.tsx, vite.config.ts.
Мета-промт «0» из шапки этого файла — оставить как есть (он правильный).